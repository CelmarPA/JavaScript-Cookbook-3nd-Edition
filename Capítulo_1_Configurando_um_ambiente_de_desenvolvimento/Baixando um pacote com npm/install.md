<!-- 
        Para criar um arquivo package.json para sua aplicação você
        pode usar  o comando "npm init -y", onde o parâmetro -y quer dizer yes, ele serve para criar um arquivo padrão preenchendo automaticamente o arquivo se omitir o -y o terminal irá pedir algumas informações.

        Para instalar algum pacote você pode usar o comando "npm install nome-pacote", quando um pacote é baixado os aquivos são colocados dentro de um pasta chamada node_modules.

        Para remover um pacote é só usar o comando "npm unistall nome-pacote"

        Você pode gerar um relatório dos pacotes instalados na sua aplicação usando o comando "npm list"

        O  npm também torna fácil a reinstalação dos pacotes caso tenha copiado para outro computador seu projeto contendo os arquivos package.json e package-lock.json, mas sem a pasta node_modules, bastar usar "npm install"

        O npm também permite instalar pacotes globalmente ( em uma pasta especifica do sistema, onde a mesma versão estará disponível para todas as aplicações no seu computador)

        Para ver todos os pacotes globais instalados em sua maquina use "npm list -g --depth 0"
 -->